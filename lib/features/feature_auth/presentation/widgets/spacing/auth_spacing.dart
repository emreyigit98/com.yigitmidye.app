import 'package:flutter/cupertino.dart';

class AuthSpacing extends StatelessWidget {
  const AuthSpacing({super.key});

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      height: 14,
    );
  }
}